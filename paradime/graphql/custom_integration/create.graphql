mutation addCustomIntegration(
    $logoUrl: String,
    $name: String!,
    $nodeTypes: [IntegrationNodeTypeInfo!]!
) {
    addCustomIntegration(
        logoUrl: $logoUrl,
        name: $name,
        nodeTypes: $nodeTypes
    ) {
        ok
        integrationUid
    }
}
